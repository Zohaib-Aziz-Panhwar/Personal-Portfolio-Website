# Static assets

Place your hero/portfolio photo here so it shows on the homepage.

- **This folder** is `frontend/public/` — Vite serves these files at the root URL.
- Hero section uses `/Picture.jpg` from this folder. Use that filename or update `src/components/Hero.jsx` to match.
